name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "ikqr-asp-application" #Must be unique in Heroku
          heroku_email: "kusikillya2001@gmail.com"
          usedocker: true
          stack: "container"
          docker_build_args: |
            ConnectionStrings__MySql
            SECRET_KEY
        env:
          ConnectionStrings__MySql: Host=sql11.freesqldatabase.com;Database=sql11413514;User=sql11413514;Password=2nuNQTbI1r;Port=3306
          SECRET_KEY: ${{ secrets.MY_SECRET_KEY }}
